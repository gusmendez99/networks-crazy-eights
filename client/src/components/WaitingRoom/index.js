import React from 'react';

export const WaitingRoom = ({ isHost=False, room, players }) => {

    return(
        <div>
            <h1>Waiting Room</h1>

        </div>
    );
}

export default WaitingRoom;